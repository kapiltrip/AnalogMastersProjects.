# Workspace Rules

- When referring to local files in this workspace, do not use markdown links.
- Use plain file paths in backticks instead, for example `Two-Stage Op-Amp/PLAN.md`.
- Reason: local markdown file links open in Chrome in this environment and are not useful for navigation.
- Apply this rule to all future responses in this workspace unless the user asks otherwise.
